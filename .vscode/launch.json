{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Start Bot",
            "program": "${workspaceRoot}/index.js",
            "cwd": "${workspaceRoot}"
        }
    ]
}